import { muatation } from './mutations'
import { types } from './types'
import { resolvers } from './resolvers'
import { queries } from './queries'

export const Tweet = { types, muatation, resolvers, queries };